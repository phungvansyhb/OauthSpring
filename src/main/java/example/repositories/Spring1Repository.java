package example.repositories;

import org.springframework.data.jpa.repository.JpaRepository;
import example.models.Spring1;

public interface Spring1Repository extends JpaRepository<Spring1, Long> {
}